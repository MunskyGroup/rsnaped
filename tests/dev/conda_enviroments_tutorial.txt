conda --version

enviroment.yml # txt file that saves the environment

# this will create an enviroment for rsnaped
conda env create --file python=3.8 rsnaped_env.yml

# ativate
conda activate rsnaped_env
    # add packages.
    pip install
    conda install
    
# deactivate
conda deactivate

# to share
conda env export --file 

# to load an envitoment
conda env create -f environment.yml

# updating env
$ conda env update --prefix ./env --file environment.yml  --prune

# ativate
conda activate rsnaped_env

# using pip
pip install -r rnsnaped/requirements.txt

